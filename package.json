{
  "name": "english-tutor-mexico",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-sitemap.mjs",    "preview": "astro preview",
    "deploy": "astro build && npx gh-pages -d dist"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^5.0.0"
  }
}
